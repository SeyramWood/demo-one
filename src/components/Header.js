import React from "react";

const Header = () => {
  return (
    <header className="header">
      <a href="/">
        <span>{`<Daily/> `}</span>
        <span>Planner</span>
      </a>
    </header>
  );
};

export default Header;
